{{#site-header as |nav|}}
  {{nav.link 'Get Started' 'get-started'}}
  {{nav.link 'Tools' 'tools'}}
{{/site-header}}

<div class="l-page">
  {{outlet}}
</div>

{{#if (not isStaff)}}
  {{auth-dialog onSubmit=(action 'authenticate')}}
{{/if}}
